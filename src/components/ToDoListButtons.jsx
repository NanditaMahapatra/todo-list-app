import React, { useState } from "react";

export default function ToDoListButtons(props){
    
    const addListItem = () => {
        
    }

  
    return(
        <div>
            <button className= "buttons" id = "add" onClick = {addListItem}>add</button>
            
        </div>
    )
}